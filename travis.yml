language: python
python:
  - "3.6"
  - "3.7"

install:
  - pip install -r requirements.txt

script:
  - python test.py

deploy:
  provider: pypi
  login: $PYPI_LOGIN
  password: $PYPI_PASSWORD
  on:
    branch: release